[pytest]
testpaths = tests
# tests/ holds the fast pure-logic tests; tests/e2e boots a real app process and
# a mock Plex server, so it is slower but exercises the whole stack.
python_files = test_*.py
asyncio_mode = auto
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
